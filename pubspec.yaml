name: floating_speed_dial
description: "A Flutter package that provides a customizable floating speed dial widget."
version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

repository: https://github.com/hennie-yun/floating_speed_dial.git # 패키지 소스 코드 저장소 링크를 추가하세요.